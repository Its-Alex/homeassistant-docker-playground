# Écoute sur toutes les interfaces (connexions de partout)
listener 1883 0.0.0.0
protocol mqtt

listener 9001 0.0.0.0
protocol websockets

# Pas d'authentification
allow_anonymous true

# Persistance et logs
persistence true
persistence_location /mosquitto/data/
